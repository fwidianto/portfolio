# Ariadne V4 — Telegram Concept A Implementation

**Status:** Approved bounded implementation task  
**Concept:** `TELEGRAM_CONCEPT_A_APPROVED.md`  
**Visual artifact:** `concepts/TELEGRAM_CONCEPT_A_MESSAGE_JOURNEY_LOOP.svg`  
**Purpose:** Validate Ariadne's consolidated concept, geometry, vision, and fidelity gates using the owner-approved Message Journey Loop.

## Runtime routing

This remains one Ariadne task. For the Builder phase use the already selected efficient Builder model unless the orchestrator explicitly changes it before the run.

The Builder must not silently switch model/provider.

This packet authorizes **Phase B — implementation** and **Phase C — deterministic geometry QA** only.

After those phases, STOP for a fresh vision-capable review. Do not self-certify visual quality if the Builder cannot see images.

## Starting-state safety

The isolated worktree is expected at:

`C:\Users\fauzan\Documents\GitHub\portofolio-ariadne-pilot`

It contains the uncommitted v1–v3 implementation in `index.html` and `CSS/main.css`.

Preserve those local changes.

Before implementation:

1. run `git status --short`;
2. confirm local implementation changes are expected;
3. fetch `origin`;
4. inspect `git diff --name-only HEAD..origin/agent/portfolio-skill-experiment`;
5. STOP if the remote diff contains `index.html`, `CSS/main.css`, JavaScript implementation files, or another unexpected product-code change;
6. otherwise fast-forward the methodology/concept/task files while preserving local implementation changes;
7. do not stash, reset, clean, discard, or overwrite the local v1–v3 evidence.

## Read before editing

Use `$shared-frontend-visualization-specialist` and load:

- `AGENTS.md` and its Read First authorities;
- `.agents/skills/shared-frontend-visualization-specialist/SKILL.md`;
- `.agents/skills/shared-frontend-visualization-specialist/methods/GEOMETRY_AND_MOTION_QA.md`;
- `.agents/skills/shared-frontend-visualization-specialist/methods/VISUAL_FIDELITY_REVIEW.md` for implementation-fidelity preparation;
- `.design/homepage-v2-skill-experiment/TELEGRAM_CONCEPT_A_APPROVED.md`;
- `.design/homepage-v2-skill-experiment/concepts/TELEGRAM_CONCEPT_A_MESSAGE_JOURNEY_LOOP.svg`;
- `.design/homepage-v2-skill-experiment/ARIADNE_V4_TELEGRAM_VALIDATION.md`.

The concept gate is already passed by Fauzan. Do not regenerate concepts during this run.

## Exact visual outcome

Add one **Telegram Codex Controller representative chapter** following the existing systems transition and Odoo chapter, before the preserved lower homepage continues.

The chapter must communicate:

> A bounded request enters from Telegram, passes through explicit control and bounded work, becomes observable, completes, and returns control to the owner.

The chapter should belong to the current Living System Editorial page while remaining visually distinct from Odoo.

## Approved composition

Preserve the approved Concept A structure:

```text
Light editorial chapter

Telegram Codex Controller heading + concise support

Dark navy signature controller canvas
  Telegram
  → Controller
  → Project Worker
  → Codex Task
  → Result
  ↘ closed return loop back to Telegram / owner

Observable-state strip
  Permission
  Worker State
  Task State
  Usage / Model
  Completion / Handoff

Transition back to preserved lower homepage
```

Do not add fake dashboard chrome merely to make the dark canvas look more complex.

## Content truth

Use only approved public-safe concepts:

- Telegram long polling as request entry;
- controller-owned Codex App Server connection over stdio;
- explicit permission/routing control;
- bounded project workers/tasks;
- observable worker/task/model/usage/completion state;
- queue/dedupe/concurrency/recovery concepts only where needed and truthfully stated;
- result/completion/handoff returns control to Fauzan.

Do not expose private identifiers, secrets, logs, prompts, or invented metrics.

No numeric success rate, active-worker count, queue count, response time, usage percentage, or performance metric unless already explicitly approved elsewhere for public use.

## Motion behavior

The primary motion is one signal traveling the closed loop.

Preferred implementation:

- one SVG visible path;
- the moving token references that exact SVG path as its motion source whenever technically possible;
- request phase uses the existing page/system blue family;
- after reaching Result, the token may transition to a restrained completion green and traverse the return segment;
- stage nodes may gain temporary emphasis as the token reaches them;
- the loop repeats calmly rather than rapidly.

Do not add static chevrons along the running forward path. The token owns direction.

A reduced-motion version may use static numbering, labels, and one restrained directional affordance where needed.

## Geometry hard gate

For the primary loop:

```text
visible path
=
animation path
=
shared SVG geometry source
```

This is a hard implementation requirement unless the Builder reports a concrete technical blocker before proceeding.

Geometry evidence must include:

- exact path element / source used;
- exact animated token reference point;
- transform origin / coordinate-space explanation;
- approximately 0%, 25%, 50%, 75%, 100% samples;
- loop/reset sample;
- desktop/tablet/mobile samples;
- maximum observed center-to-path deviation where measurable;
- confirmation that node anchors and loop endpoints are intentional.

If the token is visibly or technically detached from the claimed path, the Geometry Gate is **BLOCKED**. Do not polish around it.

## Responsive behavior

### Desktop — 1440 × 900

Use the full horizontal loop and return segment as the signature visual.

### Tablet — 768 × 1024

Recompose intentionally if horizontal density becomes weak. Preserve the closed-loop relationship.

### Mobile — 390 × 844

Do not miniaturize the desktop canvas. Use a vertical/staged closed journey that preserves request → controlled work → result → return.

The mobile geometry may use a separate SVG path if the layout genuinely changes, but its visible and animated path must still share one geometry source.

## Writable surface

Expected primary implementation files:

- `index.html`;
- `CSS/main.css`.

JavaScript is **not pre-authorized**.

If JavaScript is materially needed, STOP before editing it and report:

1. exact file;
2. exact behavior enabled;
3. why SVG/HTML/CSS is insufficient or fragile;
4. reduced-motion/fallback behavior;
5. whether the change alters any existing behavior outside the Telegram chapter.

No dependencies.

## Protected areas

Do not redesign:

- hero / identity;
- Odoo chapter;
- Professional Experience;
- Business Outputs;
- Thinking;
- skills evolution;
- Website Updates;
- Contact;
- footer;
- project detail pages;
- analytics / SEO / deployment.

A tiny continuity-spacing adjustment immediately adjacent to the Telegram chapter is allowed only if necessary to insert the chapter cleanly and must be reported.

Do not commit, push, merge, deploy, switch branch after setup, or touch the original dirty `portofolio` workspace.

## Technical validation required before handoff to vision review

- branch / HEAD / worktree state;
- exact changed-file list;
- desktop `1440 × 900` render;
- tablet `768 × 1024` render;
- mobile `390 × 844` render;
- at least 3 meaningful motion frames plus loop/reset evidence;
- reduced-motion render/state;
- keyboard-focus check on any Telegram CTA/control if present;
- no horizontal overflow;
- no blocking console errors;
- geometry gate evidence as defined above;
- concept-fidelity preparation: identify at least five items the fresh visual reviewer should compare against the approved SVG/spec.

## Vision boundary

If the Builder model cannot inspect images:

```text
Visual review: BLOCKED IN BUILDER PHASE
```

That is expected for this routing experiment.

Do not substitute DOM coordinates, PIL density, computed styles, or geometry metrics for image-based design judgment.

Produce screenshots/evidence, then STOP so the Global Orchestrator can route them to a fresh vision-capable reviewer.

## Completion report for Builder phase

```text
Outcome:
Model/provider used:
Concept implemented: Message Journey Loop
Changed files:
Protected areas unchanged:
How the implementation matches the approved concept:
Motion meaning:
Geometry source:
Geometry samples / max deviation:
Responsive geometry approach:
Reduced-motion equivalent:
Technical validation performed:
Screenshots/evidence produced:
JavaScript approval requested: YES / NO
Scope drift: YES / NO
Status:
- Concept gate: PASS (owner-approved before this run)
- Implementation: YES / PARTIAL / NO
- Geometry gate: PASS / BLOCKED
- Vision gate: PENDING EXTERNAL REVIEW
- Fidelity gate: PENDING EXTERNAL REVIEW
- Technical: COMPLETE / PARTIAL / BLOCKED
Known limitations:
Stop condition reached: YES / NO
```

## Stop condition

Stop after the bounded Telegram chapter is implemented, deterministic Geometry QA is complete, and the screenshot/motion evidence is ready for a fresh vision review.

Do **not** perform the visual correction pass yet. That pass is only authorized after the fresh vision reviewer produces a mismatch ledger and Fauzan/orchestrator routes the findings back to the Builder.
